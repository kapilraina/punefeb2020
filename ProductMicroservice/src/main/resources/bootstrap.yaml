spring:
  cloud:
    config:
      uri:
      - ${productms.cloud.config.server:http://localhost:8888}
      fail-fast: false
  application:
    name: productms
  profiles:
    active: ${productms.spring.profiles.active:dev} 
