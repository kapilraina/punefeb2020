---
spring:
  profiles:
    active: ${webauditms.activeprofiles:dev}
  application:
    name: webauditms
  cloud:
    config:
      uri: ${webauditms.zone.url:http://configserver:8888}
      fail-fast: true
